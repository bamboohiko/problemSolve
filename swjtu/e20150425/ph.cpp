#include<iostream>
#include<cstdio>
#include<map>
#include<set>
#include<vector>
#include<stack>
#include<queue>
#include<string>
#include<cstring>
#include<algorithm>
#include<cmath>
using namespace std;
void build()
int main()
{
	int n,q;
	while (scanf("%d\n",&n) != EOF) {
		for (int i = 1;i <= n; i++) scanf("%d",&a[i]);
		scanf("%d",&q);
		int e,a,b,k,c;
		for (int i = 1;i <= q; i++) {
			scanf("%d",&e);
			if (e == 1) {
				for (int i = 1;i <= 10; i++) 
			}
		} 
	}
	return 0;
}
