#include<cstdio>
using namespace std;
struct point{
	long long x,y,z;
};
point p[1010];
int main()
{int n;
	while (cin >> n)
		{for (int i = 1;i <= n; i++)
			 scanf("%lld%lld%lld",&p[i].x,&p[i].y,&p[i].y);
		for (int i = 1;i <= n; i++)
			{
			}
		}
	return 0;
}

