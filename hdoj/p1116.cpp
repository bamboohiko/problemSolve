#include<iostream>
#include<cstdio>
#include<string>
using namespace std;
int poss()
{for (int i)
}
int main()
{freopen("p1116.in","r",stdin);
 freopen("p1116.out","w",stdout);
	int t,n;
	cin >> t;
	for (int j = 1; j <= t; j++ )
		{memset(route,0,sizeof(route));
		cin >> n;getchar;
		for (int i = 1; i <= n; i++ )
			{cin >> st;
			frout=dic[st[0]];vin[frout]++;
			behind=dic[st[length(st)]];vout[behind]++;
			route[frout,behind]++;
			}
		cout << ans[poss] << endl;
		}
	return 0;
}
