#include<iostream>
#include<cstdio>
#include<map>
#include<set>
#include<vector>
#include<stack>
#include<queue>
#include<string>
#include<cstring>
#include<sstream>
#include<algorithm>
#include<cmath>
using namespace std;
int Q[100100];
int main()
{
    int t;
    for (int turn = 1;turn <= t; turn++) {
        scanf("%d",&n);
        int l,r,ans = 0;
        for (int i = 1;i <= n; i++) {
            scanf("%d",a[i]);
            if (a[i] <= a[i-1]) {
                sum = 0;
                ans++;
            } else {
                if (sum > 0)
            }
        }
    }
	return 0;
}
