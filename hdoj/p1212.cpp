#include<cstring>
#include<cstdio>
#include<iostream>
using namespace std;
const BASE=1000000;
char a[1010]
{int b;
	while (cin >> a >> b)
		{int len = strlen(a),ans = 1;
		 int n = (len - 1)/6 +1
			for (int i = 1;i >= ; i++)
				sscanf()
		}
	return 0;
}
