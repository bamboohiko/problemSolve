#include<cstdio>
#include<iostream>
#include<vector>
#include<set>
using namespace std;
vector<int> v[];
int main()
{int a,b,k=0;
	while (1)
		{v.clear();
		cin >> a >> b;
		if ((a < 0) && (b < 0)) break;
		while ((a) && (b))
			{
			}
		printf("Case %d is not a tree.",++k);
		}
	return 0;
}
